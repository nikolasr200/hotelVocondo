{% extends 'base.html.twig' %}

{% block title %}RoomType{% endblock %}

{% block body %}
    <h1>RoomType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room_type.id }}</td>
            </tr>
            <tr>
                <th>Beds</th>
                <td>{{ room_type.beds }}</td>
            </tr>
            <tr>
                <th>Amenity1</th>
                <td>{{ room_type.amenity1 }}</td>
            </tr>
            <tr>
                <th>Amenity2</th>
                <td>{{ room_type.amenity2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_room_type_index') }}">back to list</a>

    <a href="{{ path('app_room_type_edit', {'id': room_type.id}) }}">edit</a>

    {{ include('room_type/_delete_form.html.twig') }}
{% endblock %}
